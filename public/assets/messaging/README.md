# Conversation background

Drop a photo named `background.jpg` in this folder to set the background that appears
in the conversation once the first message has been sent. It's read directly from here —
no code change needed to swap it.

Until a photo is added, a gradient in the app's own palette is used instead, so the
conversation still looks intentional with nothing here.
